package com.example.hooptracker.data.local.entity

/**
 * Enum que indica el estado de un partido: no iniciado, en juego o finalizado.
 */
enum class MatchStatus {
    NOT_STARTED,
    IN_PROGRESS,
    FINISHED
}
